var config = {
    port: 80,
    mongodb: {
        host: "www.example.com",
        port: 54321,
        user: "admin",
        password: "password",
        db: "main"
    }
}

export default config;